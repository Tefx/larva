# Pi Tab Autocomplete Real TUI Smoke / Limitation Evidence

Generated by integration-verifier for step `pi_tab_autocomplete_real_tui_smoke_or_limit`.

## Mandatory refs read

- `contrib/pi-extension/README.md` lines 108-145 document `/larva-persona` Tab completion: command-level completer is preserved, a narrow `ctx.ui.addAutocompleteProvider` TUI provider is installed when available, only `/larva-persona <prefix>` is intercepted, forced and regular completion share the same path, other input delegates to Pi, and malformed/failed `larva list --json` returns `null`.
- `contrib/pi-extension/README.md` lines 163-170 document real-Pi scenario disposition: real-Pi scenarios skip if Pi is unavailable; RPC observability gaps xfail with evidence; plugin load/slash-command liveness/product failures must fail rather than hide behind unconditional skips.
- `contrib/pi-extension/larva.ts` lines 281-289 implement prefix `startsWith` persona candidate mapping to `{value,label,description}`.
- `contrib/pi-extension/larva.ts` lines 316-349 implement `/larva-persona <prefix>` line parsing, base-provider delegation, provider installation via `ctx.ui.addAutocompleteProvider`, and null-on-error behavior.
- `contrib/pi-extension/larva.ts` lines 352-377 register `/larva-persona` and preserve the command-level `getArgumentCompletions`/legacy `complete` hook.
- `contrib/pi-extension/larva.ts` lines 927-943 call command registration and TUI autocomplete provider registration during extension initialization.

## Commands run

All commands ran in isolated worktree `/Users/tefx/Projects/larva/.vectl/worktrees/pi_tab_autocomplete_real_tui_smoke_or_limit`.

| Command | Exit | Key raw evidence |
|---|---:|---|
| `node scripts/pi-extension-runtime-smoke.mjs --scenario get-commands --json` | 0 | Pi available `true`, extension flag `-e`, RPC supported `true`, `get_commands` success `true`, commands include `larva-persona`, stderr empty. |
| `node scripts/pi-extension-runtime-smoke.mjs --scenario slash-status --persona vectl-planner --json` | 0 | Pi available `true`, RPC supported `true`, prompt success `true`, UI status `larva: vectl-planner`, notify `Larva persona active: vectl-planner`, stderr empty. |
| `node scripts/pi-extension-autocomplete-smoke.mjs --case tab-force --prefix vectl --real-pi-tui` | 0 | Script returned provider-harness JSON for `case: tab-force`; it did not launch a real Pi TUI or synthesize keyboard Tab despite the extra flag. Values: `vectl-planner`, `vectl-reviewer`; `provesArgumentPrefix: true`, `exactShape: true`. |
| `node scripts/pi-extension-autocomplete-smoke.mjs --case tab-regular --prefix vectl` | 0 | Provider-harness JSON for `case: tab-regular`, same values and shape assertions as forced path. |
| `node scripts/pi-extension-autocomplete-smoke.mjs --case delegate-other-input` | 0 | Non-`/larva-persona` input delegated to base provider; `delegated: true`. |
| `node scripts/pi-extension-autocomplete-smoke.mjs --case list-failure --prefix vectl` | 0 | Failed/malformed list returned `null`; `noCrash: true`. |

## Limitation record

`scripts/pi-extension-autocomplete-smoke.mjs` accepts arbitrary `--*` keys but has no branch that interprets `--real-pi-tui`, spawns `pi` in TUI mode, attaches a pseudo-terminal, enters `/larva-persona vectl`, presses Tab, or observes rendered editor-completion UI/provider calls. Therefore the requested real TUI Tab command is an attempted automation command, but the returned evidence is wrapper/provider harness evidence, not P10 live TUI proof.

The wrapper harness remains the minimum reliable evidence for provider behavior in this worktree because it initializes the real extension module, verifies `/larva-persona` command registration is preserved, captures the installed `ctx.ui.addAutocompleteProvider` provider, exercises forced/regular provider calls, base delegation, and list-failure behavior using the deterministic fake Larva CLI. This does not intersect with the real Pi RPC liveness checks above, which separately prove Pi binary availability, extension load, command visibility, and slash-command execution/status through Pi RPC.

Future real TUI closure requires a supported automation seam: a PTY or Pi test API capable of launching interactive `pi` with this extension, injecting `/larva-persona vectl<Tab>`, and observing either actual rendered completion candidates or Pi runtime telemetry proving the installed editor autocomplete provider was called by Tab.


## Corrected closure decision

Re-evaluation result: this evidence step is closable with `gate_open_allowed: true` because all closure obligations that are supportable in the current environment were met: real Pi RPC liveness is proven for plugin load/`get_commands`, real Pi slash command execution/status is proven for `/larva-persona vectl-planner`, and the wrapper/provider path is proven for forced completion, regular completion, delegation, and list-failure behavior.

The true rendered TUI Tab observation is explicitly `EXCLUDED_OR_DEFERRED` for this step, not a product blocker, because the only missing surface is an automation seam: the current smoke script accepts `--real-pi-tui` syntactically but does not launch Pi in an interactive TUI, attach a PTY, synthesize Tab, or observe rendered completion candidates. That limitation is environmental/tooling-specific and non-intersecting with the proven Pi RPC/plugin/slash liveness and wrapper provider behavior. Direct command completer or command visibility evidence remains insufficient for rendered TUI Tab closure; it is used only as non-intersection/wrapper-path support.

Corrected gate fields for orchestrator consumption:

- `gate_open_allowed`: `true`
- `orchestrator_action_hint`: `COMPLETE`
- `verdict`: `PASS`
- `blockers`: `[]`
- `true_rendered_tui_tab_observation`: `EXCLUDED_OR_DEFERRED` pending future PTY/Pi TUI automation seam
